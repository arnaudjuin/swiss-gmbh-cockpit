services:
  app:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-demo}
      SEED_DEMO: ${SEED_DEMO:-1}
    volumes:
      - ./data:/app/data
