/** @type {import('next').NextConfig} */
// Self-contained app: every /api/* endpoint is served by this project's own
// route handlers in app/api — there is no external backend to proxy to.
const nextConfig = {};

export default nextConfig;
